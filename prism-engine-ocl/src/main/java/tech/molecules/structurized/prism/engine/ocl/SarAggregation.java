package tech.molecules.structurized.prism.engine.ocl;

/** Aggregation applied to numeric observations contributing to one SAR bucket. */
public enum SarAggregation {
    BEST,
    MEAN,
    MEDIAN,
    MIN,
    MAX
}
