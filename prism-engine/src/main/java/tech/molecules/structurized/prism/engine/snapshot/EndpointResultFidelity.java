package tech.molecules.structurized.prism.engine.snapshot;

/** Scientific fidelity available behind endpoint display cells. */
public enum EndpointResultFidelity {
    NONE,
    DISPLAY_ONLY,
    SYNTHESIZED,
    PARTIAL,
    FULL
}
